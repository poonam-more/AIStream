//
//  ContentView.swift
//  AIStream
//
//  Created by Poonam More on 12/02/26.
//

import SwiftUI
/*
struct ContentView: View {
    
    var body: some View {
        TabView {
            NavigationStack {
                ChatView(viewModel: ChatViewModel())
            }
            .tabItem {
                Label("Home", systemImage: "house")
            }
            
            ProjectsView()
                .tabItem {
                    Label("Projects", systemImage: "folder")
                }
            
            HistoryView(service: HistoryService(baseURL: APIClient.baseURL, accessToken: KeychainHelper.shared.getAccessToken() ?? ""), chatViewModel: ChatViewModel(), navigationPath: <#Binding<NavigationPath>#>)
                .tabItem {
                    Label("History", systemImage: "clock.arrow.circlepath")
                }
        }
    }
}

#Preview {
    ContentView()
}
*/
